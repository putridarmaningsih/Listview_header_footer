import React from 'react';
import MainApp from './src/MainApp'; //tempat import

export default class App extends React.Component { //extends pewarisan
  render() {
    return ( //mengeluarkan apa yang dibuat
        <MainApp />
    );
  }
}
